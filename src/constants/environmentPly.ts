/**
 * Default PLY path under `public/environments/scene.ply`.
 * Large files are stride-downsampled for display (see `PLY_DISPLAY_VERTEX_BUDGET` in `threeScene.ts`).
 */
export const DEFAULT_ENVIRONMENT_PLY_URL = '/environments/scene.ply';
